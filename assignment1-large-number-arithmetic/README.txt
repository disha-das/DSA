The executable assign1_exec takes as input the file "input.txt"

The file should contain the operands in two lines.
The first line is considered the first operand and the second line is the second operand.
<operand1>
<operand2>

The output based on the operator selection is printed on cosole while executing.

Addition, Subtraction, Multiplication and Division operators can be used.
